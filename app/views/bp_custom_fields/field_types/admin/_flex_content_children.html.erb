<div class="nested-fields layout_fields">
  <div class='bpcf-field form-group'>
  <%= f.hidden_field :field_template_id %>

  <%= f.fields_for :children do |inside_field| %>
	  <%= basic_field(inside_field, inside_field.object.field_template) %>
  <% end %>
</div>
  <%= link_to_remove_association( "Remove #{f.object.name}", f)%>
</div>  